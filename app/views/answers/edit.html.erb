<div class="page-header">
  <h1>Edit answer</h1>
</div>
<div class="jumbotron">
    <h3><%= @answer.question.text %></h3>
</div>
<%= render 'form' %>
<div class="container">
  <%= link_to("Remove answer", @answer, method: :delete, class:"btn btn-default") if can? :destroy, @answer %>
</div>