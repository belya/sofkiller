<div class="page-header">
  <h1><%= @user.name %> (<%= @user.status %>)</h1>
</div>
<div class="container">
  <p>
    <%= link_to("Edit profile", edit_user_path(@user), class:"btn btn-primary pull-right") if can? :manage, @user %>
  </p>
  <div class="container">
  <div class="page-header">
    <h2>Questions</h2>
  </div>
    <%= render 'questions/questions', questions: @user.questions %>
  </div>
</div>